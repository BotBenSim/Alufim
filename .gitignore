# Python virtualenv (image-processing scripts only; not part of the game)
.venv/

# rembg AI model cache (downloaded on demand, 170MB+)
.u2net/

# Generated offline single-file build (22MB, all images inlined as base64)
alufim.standalone.html

# OS / editor cruft
.DS_Store

# Next.js app (dependencies and build output)
web/node_modules/
web/.next/
web/out/

# Generated agent files — rebuilt from playbooks/ via `make agents` (single source of truth)
.cursor/rules/knowledge.mdc
.cursor/commands/
.cursor/skills/
.claude/
/CLAUDE.md
